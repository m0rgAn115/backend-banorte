spring:
  application:
    name: backend
  datasource:
    url: jdbc:mysql://34.67.38.134:3306/backend
    username: morgan
    password: morgan
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  cloud:
    config:
      enabled: false

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
    shutdown:
      enabled: true
  info:
    env:
      enabled: true

info:
  app:
    name: "usuarios ms"
    description: "Microservicio encargado de la gestion de usuarios"
    version: "1.0.0"